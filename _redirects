# Redirect default Netlify subdomain to primary domain
https://h404bi.netlify.com/* https://www.h404bi.com/:splat 301!

# Redirect old permalinks to new permalinks (post)
/blog/:year/:month/:day/:slug.html /blog/:year/:month/:slug 301!

# Redirect old permalinks to new permalinks (page)
/:slug.html /:slug
